# AsihHealth - YouTube Automation (Cost Efficient - Bahasa Indonesia)
# Python 3.11+

# === LLM (Primary: Ollama - Gratis Total) ===
ollama>=0.3.0

# === TTS (edge-tts - Gratis, Kualitas Terbaik untuk ID) ===
edge-tts>=6.1.12

# OpenAI TTS (opsional, via TTS_PROVIDER=openai) — reliable cloud API, tidak butuh extra package
# (menggunakan `requests` yang sudah ada). Butuh OPENAI_API_KEY di .env. Lihat .env.example
# Catatan: Penggabungan chunk audio untuk script panjang menggunakan FFmpeg (bukan pydub)
# agar kompatibel dengan Python 3.13+ (audioop dihapus dari stdlib).


# === Subtitle (faster-whisper - lokal & akurat) ===
faster-whisper>=1.0.0
# torch (otomatis terinstall via faster-whisper, pakai CPU version kalau GPU terbatas)

# === Video Processing ===
# FFmpeg dipanggil via subprocess (paling efisien)
# Alternatif ringan:
ffmpeg-python>=0.2.0

# === Image / Thumbnail + Stock Downloader ===
Pillow>=10.4.0
requests>=2.32.0

# === YouTube Upload ===
google-api-python-client>=2.140.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# === Utilities ===
python-dotenv>=1.0.1
pydantic>=2.9.0
rich>=13.9.0          # Beautiful CLI output
typer>=0.12.5         # CLI framework
schedule>=1.2.2       # For daily scheduler loop

# TTS Local (opsional tapi direkomendasikan untuk reliability - edge-tts sering bermasalah)
# 
# ⚠️  PENTING: TTS package (untuk xtts) HANYA support Python 3.7 sampai 3.10
#    Kalau kamu pakai Python 3.11+ (termasuk 3.14) → "No matching distribution found for TTS"
#    → Lihat QUICKSTART.md bagian "PENTING: Python Version"
#
# Alternatif super gampang tanpa install berat: TTS_PROVIDER=openai (lihat .env.example + OPENAI_API_KEY)
#
# pip install piper-tts   # untuk TTS_PROVIDER=piper (ringan, cepat, offline). Butuh espeak-ng di Windows.
# pip install TTS         # untuk TTS_PROVIDER=xtts (kualitas bagus, multilingual, ~2GB model)
#   Windows CPU (pakai Python 3.10): py -3.10 -m pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# === Dashboard ===
streamlit>=1.38.0

# === Optional: Stock Footage Downloader ===
# requests>=2.32.0
# httpx>=0.27.0

# === Dev / Testing (optional) ===
# pytest>=8.3.0
# black>=24.10.0
